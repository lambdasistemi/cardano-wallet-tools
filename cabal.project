index-state: 2026-02-17T10:15:41Z

packages:
  .

-- v0 ships only the pure `wallet-core` selection library, which depends
-- on base/containers/text and the standard test stack. The
-- cardano-tx-tools / cardano-ledger wiring (and its CHaP
-- source-repository-packages) lands with the ledger-typed wrappers in a
-- follow-up ticket, gated on GHC-WASM/GHC-JS cross-compilation per the
-- constitution.
